import type { FC } from 'react';
import type { GameProps } from '../types';

export const GameSandbox: FC<GameProps> = ({
  gameId,
  onGameStart,
  onScoreUpdate,
  onGameOver,
}) => {
  return null;
};
